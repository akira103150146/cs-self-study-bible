---
after: 2
title_zh: 列運算不改變解集
---
## 定理
If two augmented matrices are row equivalent, then the two linear systems they represent have exactly the same solution set.

## 為什麼值得證
整個消去法的正當性都靠這一句:我們把矩陣改了一次又一次,憑什麼最後讀出來的解,就是原本那一題的解?

## 關鍵想法
每一種列運算都能用另一個列運算**復原**。舊方程組的解一定滿足新方程組;反過來,新方程組的解透過復原,也一定滿足舊方程組。兩邊互相包含,所以解集一模一樣。

## 證明
一連串列運算不改變解集,只要證明**每一次**列運算不改變解集就夠了(一步一步串起來)。所以只看單一次運算,分三種。

**Replacement:$R_2 \leftarrow R_2 + cR_1$。** 把原本第 1、2 式簡寫成 $E_1 = b_1$、$E_2 = b_2$(其他式子不動)。新方程組的第 2 式是 $E_2 + cE_1 = b_2 + cb_1$。

- **舊解 ⇒ 新解**:若 $\mathbf{s}$ 讓 $E_1 = b_1$、$E_2 = b_2$ 成立,兩式相加(第 1 式乘 $c$)就得到 $E_2 + cE_1 = b_2 + cb_1$,所以 $\mathbf{s}$ 滿足新方程組。
- **新解 ⇒ 舊解**:若 $\mathbf{s}$ 讓 $E_1 = b_1$ 與 $E_2 + cE_1 = b_2 + cb_1$ 成立,後者減去 $c$ 倍的前者,得到 $E_2 = b_2$,所以 $\mathbf{s}$ 滿足舊方程組。

這裡的「減去 $c$ 倍」正是復原用的列運算 $R_2 \leftarrow R_2 - cR_1$。

**Interchange。** 方程式本身一條都沒變,只是換了順序;「同時滿足每一條」和順序無關,解集當然相同。

**Scaling:$R_1 \leftarrow cR_1$,$c \neq 0$。** 若 $E_1 = b_1$ 成立,兩邊乘 $c$ 得 $cE_1 = cb_1$;反過來,因為 $c \neq 0$,可以兩邊除以 $c$ 回到 $E_1 = b_1$。**$c \neq 0$ 在這裡被用到**——少了它,就沒辦法從新方程組走回舊方程組。

三種運算都不改變解集,所以任何一連串列運算都不改變解集。$\blacksquare$

**課堂上只示範 replacement 的雙向論證**,另外兩種口頭帶過即可。考試不會要求寫出這個證明,會考「scaling 為什麼要求 $c \neq 0$」這類「哪個前提被用到」的題目。
